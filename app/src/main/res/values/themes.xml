<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Myself" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Myself" parent="Base.Theme.Myself" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/whiteApp</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logoe</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Myself</item>
    </style>
</resources>